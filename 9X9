/*【循环嵌套】编写程序，输入一个整数h，打印指定行数（h）的九九乘法表。完整的九九乘法表打印形式如下：（说明：第一个乘数占两格，结果占两格）
 1*1= 1
 2*1= 2 2*2= 4
 3*1= 3 3*2= 6 3*3= 9
 4*1= 4 4*2= 8 4*3=12 4*4=16
 ...
 9*1= 9 9*2=18 9*3=27 …               9*9=81
例如：输入：3
     输出:  1*1= 1
            2*1= 2 2*2= 4
            3*1= 3 3*2= 6 3*3= 9
【测试数据有多组，每组输出结果后必须换行】*/
#include<stdio.h>
int fun( )
{
	int h;
	int i,j,k;
	scanf("%d",&h);

	for(i = 1;i <= h;i++)
		{
			for(j = 1;j <= i;j++)
			{
				k = i * j;
				printf("%d*%d=%d ",i,j,k);
			}
			printf("\n");
		}
 }
 int main( )
 {
 	fun( );
 }
