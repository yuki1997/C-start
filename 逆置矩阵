/*矩阵转置。输入一个N*N的矩阵（2<=N<=10），将其转置后输出。要求：不得使用任何数组(就地逆置)。输入：第一行是整数n，接下来的n行每行输入n个整数   输出：逆置后的n*n的矩阵*/
#include<stdio.h>
int f(int N)
{
  int a[N][N];
  int i,j;
  if(N >= 2&&N <= 10){
  for(i = 0;i < N;i++){
    for(j = 0;j < N;j++){
        scanf("%d",&a[i][j]);
        }
      }
    }
  for(j = 0;j < N;j++){
    for(i = 0;i < N;i++){
      printf("%d",a[j][i]);
    }
  }
  return 0;
}
int main()
{
  int n;
  scanf("%d",&n);
  f(n);
}
